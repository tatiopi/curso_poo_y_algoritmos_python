# generar un array (lista) ordenada 
```python
 lista = sorted([random.randint(0,100) for i in range(tamano_de_lista)])
```

 # operador terciario
 ```python
 print(f'El elemento {objetivo} {"esta"} if encontrado else "no esta"')
 ```

 # division de enteros
 ```python
 medio = (comienzo + final) // 2 
 ```